# HUI-B1 unavailable artifact fixture

This root deliberately omits every HUI-B1 manifest. The integration test uses
it to prove missing acquisition evidence fails closed with one diagnostic per
required record.
